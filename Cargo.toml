[package]
name = "jwks_server"
version = "0.0.1"
edition = "2021"

[dependencies]
rocket = { version = "0.5.0", features = ["json"] }
rsa = "0.5"
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

